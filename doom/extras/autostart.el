;;; autostart.el -*- lexical-binding: t; -*-
;;(require 'vterm)
;;
;;(add-hook 'emacs-startup-hook
;;          (lambda ()
;;            (let ((current-window (selected-window)))
;;              (vterm)
;;              (select-window current-window)))
;;          t)
